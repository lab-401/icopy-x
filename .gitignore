__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
*.ipk
*.img
*.7z
.coverage

# Build outputs (Docker-produced, not checked in)
build/proxmark3
build/lua.zip

# Screenshots (dev only)
screenshot_*.png
qemu_api_dump_*.txt
qemu_run.sh

# IDE / AI tools
.vscode/
.idea/

*.swp
*.swo

# Test artifacts
/tmp/
tests/flows/_results/

# SD card / device dumps (not project files)
Nanopicontentsfromsdc/
build_log*.txt
docker_build*.log
docker_build*.sh
fullimage.elf
ipk_build.log
proxmark3.exe

# Editor / tooling scratch (not project files)
.claude/
.kilo/
memory-bank/

# Extracted IPK / local build output (not project files)
ipk_extract/
build/docker_fw_out/
# Debug logs
icopy_debug.log

# Claude Code: ignore local/personal settings, but TRACK shared .claude/ tooling
# (workflows, agents, commands) so the team's multi-agent methodology is versioned.
.claude/settings.local.json
.claude/*.local.json
