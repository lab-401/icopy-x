# Backlight: cancel from High(2) — UP×2 to Low(0), then PWR cancel
# recovery_backlight() should restore original High level
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 2
