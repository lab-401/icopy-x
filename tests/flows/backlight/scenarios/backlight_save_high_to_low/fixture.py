# Backlight: save High(2) → Low(0) — UP×2 then M2 save
# Inverse of session 1 trace — start high, navigate to low
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 2
