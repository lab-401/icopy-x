# Backlight: cancel with recovery — start Low(0), DOWN to Middle, PWR cancel
# recovery_backlight() should restore original Low level
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 0
