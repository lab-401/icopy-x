# Backlight: save same level — start Middle(1), no navigation, M2 save
# Confirms M2 works even without changing selection
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 1
