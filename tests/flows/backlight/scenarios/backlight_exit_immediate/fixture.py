# Backlight: exit immediately — start Low(0), no navigation, PWR exit
# Tests PWR with no changes — recovery_backlight() still called
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 0
