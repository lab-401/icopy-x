# Backlight: save Low(0) → Middle(1) — DOWN×1 then M2 save
# Tests single-step navigation + save (partial traversal)
# No PM3 commands in backlight flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_BACKLIGHT = 0
