# Volume: Off(0) → DOWN once to Low(1) → PWR exit (no recovery)
# Verifies PWR does NOT save — re-entry should show Off(0)
# No PM3 commands in Volume flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_VOLUME = 0
