# Volume: Low(1) → navigate DOWN×1 to Middle(2) → M2 save
# Tests non-Off to non-Off transition (key audio stays enabled)
# No PM3 commands in Volume flow
SCENARIO_RESPONSES = {}
DEFAULT_RETURN = 1
CONF_VOLUME = 1
